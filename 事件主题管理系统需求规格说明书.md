# 事件主题管理系统需求规格说明书

## 1. 系统概述

### 1.1 项目背景
事件主题管理系统是事件分析平台的核心功能模块，旨在通过智能化的关键词筛选规则，将海量事件数据按照特定主题进行自动分类和聚合，为政府部门提供精准的事件监控、分析和决策支持。

### 1.2 系统目标
- **智能分类**：基于多层关键词规则自动筛选和分类事件
- **实时监控**：提供主题事件的实时统计和趋势分析
- **灵活配置**：支持复杂的筛选条件和个性化配置
- **异动预警**：自动识别异常数据波动并提供预警

### 1.3 核心价值
- 提高事件处理效率，减少人工筛选成本
- 支持多维度数据分析，辅助决策制定
- 实现事件监控自动化，提升响应速度
- 建立标准化的事件分类体系

## 2. 功能需求

### 2.1 主题创建功能

#### 2.1.1 基本信息配置
**功能描述**：管理员可创建新的事件主题，定义主题的基本属性和筛选规则。

**输入项**：
- **主题名称**（必填）：长度限制50字符，用于标识主题
- **主题描述**（可选）：长度限制200字符，描述主题用途和逻辑

**验证规则**：
- 主题名称不能为空
- 主题名称不能重复
- 描述字符数不能超过限制

**实现现状**：
- 前端表单对名称必填与长度做了基础校验，但后端目前未对名称长度或重复性进行二次校验，存在并发提交重复名称的风险（需在服务端补充校验）。
- 创建接口未对描述字段做长度限制，依赖前端输入框 `maxLength=200` 限制；若通过 API 直接调用需自行控制长度。

#### 2.1.2 关键词筛选配置

##### 第一道：包含关键词筛选
**功能描述**：设置事件必须包含的关键词，支持字段级精确匹配。

**配置项**：
- **事件描述关键词**：在事件描述字段中匹配的关键词列表
- **处置结果关键词**：在处置结果字段中匹配的关键词列表

**逻辑关系**：
- 字段间关系：AND（事件描述和处置结果字段必须同时满足）
- 字段内关系：OR（同一字段内的多个关键词满足任意一个即可）

**示例**：
```
事件描述关键词：["噪音", "噪声", "吵闹"]
处置结果关键词：["已处理", "处理完毕"]
匹配条件：(事件描述包含"噪音"或"噪声"或"吵闹") AND (处置结果包含"已处理"或"处理完毕")
```

##### 第二道：过滤关键词筛选
**功能描述**：设置需要排除的关键词，避免误匹配。

**配置项**：
- **事件描述排除关键词**：在事件描述中出现则排除的关键词
- **处置结果排除关键词**：在处置结果中出现则排除的关键词

**逻辑关系**：
- 字段间关系：OR（任意字段包含排除关键词则排除整个事件）
- 字段内关系：OR（同一字段内任意关键词匹配则排除）

**示例**：
```
事件描述排除关键词：["KTV", "娱乐场所"]
处置结果排除关键词：["无需处理", "重复投诉"]
排除条件：(事件描述包含"KTV"或"娱乐场所") OR (处置结果包含"无需处理"或"重复投诉")
```

##### 第三道：数据去重
**功能描述**：对筛选结果进行去重处理，避免重复统计。

**去重方式**：
- **按事件描述去重**：基于事件描述内容去除重复事件
- **按事件ID去重**：基于事件编号去除重复记录
- **不去重**：保留所有匹配的事件

**默认值**：按事件描述去重（开启状态）

**实现现状**：
- 后端 `dedup` 字段支持 `null`、`"description"`、`"event_id"` 三种取值，并据此调用 `pandas.drop_duplicates`。
- 现有前端仅暴露“按事件描述去重”开关（默认开启），无法直接配置“按事件ID去重”或“保留全部”，可通过 API 调整。

#### 2.1.3 分类配置（可选）
**功能描述**：为主题定义更细致的分类维度，支持多维度筛选。

**配置项**：
- **街镇名称**：多选下拉，限定特定区域的事件
- **事件级别**：多选下拉，筛选特定级别的事件
- **二级分类**：多选下拉，按事件类型进一步细分
- **时间范围**：日期区间选择，限定时间范围

**应用场景**：
- 区域专项监控：只关注特定街镇的噪音事件
- 等级分析：重点监控高级别事件的处理情况
- 类型细分：在噪音主题下再细分为施工噪音、交通噪音等

**实现现状**：
- 分类配置在数据模型中对应 `TopicCategory`，包含 `towns`、`levels`、`categories`、`start_time`、`end_time` 字段；现有界面支持添加多组分类条件，每组在后端以 AND 关系应用。
- `TopicCategory.keywords` 目前未在前端使用，保留向后兼容扩展。

### 2.2 主题列表功能

#### 2.2.1 列表展示
**功能描述**：以卡片式布局展示所有已创建的主题，支持快速浏览和操作。

**展示信息**：
- 主题名称和启用状态标识
- 主题描述信息
- 关键词配置摘要
- 创建时间
- 操作按钮（查看详情、统计分析、删除）

**布局特性**：
- 响应式网格布局（大屏3列、中屏2列、小屏1列）
- 悬停效果和点击跳转
- 状态标签颜色区分（启用/停用）

#### 2.2.2 关键词信息展示
**功能描述**：在列表中直观展示主题的关键词配置。

**展示格式**：
- **包含关键词**：蓝色标签，格式为"包含: 描述:关键词1、关键词2 | 结果:关键词3"
- **排除关键词**：红色标签，格式为"排除: 描述:关键词1 | 结果:关键词2"
- **去重设置**：橙色标签，显示"去重: 按描述"或"去重: 按ID"

**兼容性**：
- 支持新版字段级关键词格式
- 向后兼容旧版数组格式关键词
- 列表渲染时将自动把旧版数组转换为 `description` 字段展示；数据持久化在 `data/topics.json` 中，首次加载会完成格式迁移。

#### 2.2.3 操作功能
**操作类型**：
- **查看详情**：跳转到主题详情页面，查看匹配的事件列表
- **统计分析**：跳转到统计分析页面，查看趋势图表
- **删除主题**：删除确认后永久删除主题（不可恢复）

**交互设计**：
- 操作按钮使用图标+Tooltip提示
- 删除操作需要二次确认
- 阻止事件冒泡，避免误触

### 2.3 主题详情功能

#### 2.3.1 主题信息展示
**功能描述**：展示主题的完整配置信息和启用状态。

**展示内容**：
- 主题名称和状态标签
- 主题描述
- 关键词配置详情（包含、排除、精筛、去重）

#### 2.3.2 匹配事件列表
**功能描述**：展示符合主题筛选条件的所有事件，支持多种筛选和查询功能。

**查询功能**：
- **时间筛选**：日期范围选择器，支持自定义时间区间
- **关键词搜索**：全文搜索框，在事件编号、描述、处置结果中搜索
- **列头筛选**：支持按列进行高级筛选

**列头筛选功能**：
- **事件描述筛选**：
  - 包含关键词输入（多个关键词OR关系）
  - 排除关键词输入（多个关键词OR关系）
  - 支持空格分词和标签式输入
- **下拉多选筛选**：
  - 镇街名称多选
  - 事件级别多选
  - 二级分类多选

**表格功能**：
- 自定义列显示：用户可选择显示哪些列
- 列顺序调整：支持拖拽调整列的显示顺序
- 列宽调整：可调整列宽适应内容
- 分页展示：支持页面大小调整

#### 2.3.3 数据筛选逻辑
**前端筛选**：对已加载的事件数据进行客户端筛选
**后端筛选**：通过API参数进行服务端筛选和分页
**组合筛选**：列头筛选 + 时间筛选 + 关键词搜索的组合应用

**实现现状**：
- `GET /api/topics/{topic_id}/events` 当前支持分页、时间范围与模糊搜索（事件编号/描述/处置结果）；列头筛选（多选/包含/排除）在前端对已加载的数据进行二次过滤，属于客户端筛选。
- 描述列筛选支持“包含/排除”双列表输入，底层通过本地字符串匹配实现，不会回写到服务端。

### 2.4 统计分析功能

#### 2.4.1 概览统计
**功能描述**：提供主题事件的总体统计信息。

**统计指标**：
- **总事件数**：主题匹配的事件总数量
- **时间范围**：统计数据的时间跨度
- **最后更新时间**：数据最后同步时间

#### 2.4.2 趋势分析
**功能描述**：提供多维度的时间序列分析，支持按日/按月聚合。

**分析维度**：
- **按日统计**：
  - 每日事件数量
  - 7日移动平均线
  - 异动标识（超过均值+2σ）
- **按月统计**：
  - 每月事件数量
  - 3月移动平均线
  - 月度异动标识

**异动分析**：
- **算法思路**：以目标周期内事件数的全局均值与标准差为基础，采用“均值 + 2×标准差”作为阈值。单日事件数超过该阈值即判定为异动。前端月度视图会将日度数据按月汇总，再复用同一算法计算 3 月移动平均与月度阈值。
- **移动平均作用**：7 日（或 3 月）移动平均用于平滑短期波动，帮助管理者识别趋势变化；图表与明细表会同时展示原始值与移动平均值。
- **状态标识**：异动点在图表中以红色标注，在表格中以“异动”标签突出显示，同时保留具体数值以便快速比对。
- **阈值说明**：阈值基于当前统计区间内的全部数据动态计算，若标准差为 0（例如数据完全平坦），则不触发异动高亮。

**可视化展示**：
- **折线图**：展示最近30个时间点的趋势
- **数据表格**：详细的时间序列数据
- **异动标识**：红色标签标识异常时间点

**实现现状**：
- 后端 `GET /api/topics/{topic_id}/stats` 提供按日聚合结果、7日移动平均及全局阈值异动标识；按月统计由前端基于日数据再聚合并计算 3 月移动平均/异动。

#### 2.4.3 交互功能
**时间筛选**：支持自定义日期范围查看特定时期的统计
**视图切换**：在日统计和月统计之间切换
**数据导出**：支持统计数据的导出功能（未来扩展）

## 3. 技术实现

### 3.1 系统架构

#### 3.1.1 前端架构
- **框架**：React.js + Ant Design UI框架
- **路由**：React Router DOM
- **状态管理**：React Hooks (useState, useEffect, useMemo)
- **图表库**：@ant-design/plots (基于G2Plot)
- **时间处理**：dayjs

#### 3.1.2 后端架构
- **框架**：Python FastAPI
- **数据处理**：pandas数据分析库
- **数据存储**：JSON文件存储（可扩展为数据库）
- **API设计**：RESTful API

### 3.2 数据模型

#### 3.2.1 主题数据模型
```json
{
  "id": "主题唯一标识",
  "name": "主题名称",
  "description": "主题描述",
  "include_keywords": {
    "description": ["事件描述关键词数组"],
    "result": ["处置结果关键词数组"]
  },
  "exclude_keywords": {
    "description": ["排除事件描述关键词数组"],
    "result": ["排除处置结果关键词数组"]
  },
  "categories": [
    {
      "name": null,
      "keywords": [],
      "towns": ["街镇名称数组"],
      "levels": ["事件级别数组"],
      "categories": ["二级分类数组"],
      "start_time": "开始时间",
      "end_time": "结束时间"
    }
  ],
  "dedup": "去重方式(description|event_id|null)",
  "fine_filters": ["精筛关键词数组"],
  "enabled": true,
  "createTime": "YYYY-MM-DD HH:MM:SS"
}
```

> 兼容说明：旧版数据中 `include_keywords` / `exclude_keywords` 可能仍以字符串数组形式存储，后端在加载 `data/topics.json` 时会自动转换为字段级结构。

#### 3.2.2 事件数据模型
```json
{
  "事件编号": "事件唯一编号",
  "事件描述": "事件详细描述",
  "镇街名称": "所属街镇",
  "事件级别": "事件级别",
  "二级分类": "事件二级分类",
  "上报时间": "事件上报时间",
  "处置结果": "事件处置结果",
  "报警人信息": "报警人相关信息"
}
```

#### 3.2.3 统计数据模型
```json
{
  "topic_id": "主题ID",
  "total": "总事件数",
  "by_day": [
    {
      "date": "日期",
      "count": "当日事件数",
      "ma7": "7日移动平均",
      "anomaly": "是否异动"
    }
  ]
}
```

### 3.3 核心算法

#### 3.3.1 关键词匹配算法
```python
def match_keywords(event, include_keywords, exclude_keywords):
    """
    关键词匹配核心算法
    
    Args:
        event: 事件数据
        include_keywords: 包含关键词配置
        exclude_keywords: 排除关键词配置
    
    Returns:
        bool: 是否匹配
    """
    # 第一道：包含关键词筛选（字段间AND，字段内OR）
    include_match = True
    if include_keywords.description:
        desc_match = any(kw in event.description for kw in include_keywords.description)
        include_match = include_match and desc_match
    
    if include_keywords.result:
        result_match = any(kw in event.result for kw in include_keywords.result)
        include_match = include_match and result_match
    
    # 第二道：排除关键词筛选（字段间OR，字段内OR）
    exclude_match = False
    if exclude_keywords.description:
        desc_exclude = any(kw in event.description for kw in exclude_keywords.description)
        exclude_match = exclude_match or desc_exclude
    
    if exclude_keywords.result:
        result_exclude = any(kw in event.result for kw in exclude_keywords.result)
        exclude_match = exclude_match or result_exclude
    
    return include_match and not exclude_match
```

#### 3.3.2 异动检测算法
```python
def detect_anomaly(data_points, window_size=7):
    """
    基于移动平均和标准差的异动检测
    
    Args:
        data_points: 时间序列数据点
        window_size: 移动平均窗口大小
    
    Returns:
        list: 包含异动标识的数据点
    """
    # 计算移动平均
    for i, point in enumerate(data_points):
        start_idx = max(0, i - window_size + 1)
        window = data_points[start_idx:i+1]
        point['ma'] = sum(p['count'] for p in window) / len(window)
    
    # 计算全局统计量
    all_counts = [p['count'] for p in data_points]
    mean = sum(all_counts) / len(all_counts)
    variance = sum((x - mean) ** 2 for x in all_counts) / len(all_counts)
    std_dev = variance ** 0.5
    
    # 异动阈值：均值 + 2倍标准差
    anomaly_threshold = mean + 2 * std_dev
    
    # 标识异动
    for point in data_points:
        point['anomaly'] = point['count'] > anomaly_threshold
    
    return data_points
```

### 3.4 API接口规范

> 默认 Base URL：`http://localhost:8000/api`。接口统一返回 JSON，错误使用 FastAPI 标准 `{"detail": "..."}` 格式。

#### 3.4.1 主题管理接口
```
GET /api/topics
获取主题列表

POST /api/topics
创建新主题
Request Body: TopicCreateRequest

PUT /api/topics/{topic_id}
更新主题
Request Body: TopicUpdateRequest

DELETE /api/topics/{topic_id}
删除主题
```

- `GET /api/topics` 返回 `TopicListResponse`。
- `POST /api/topics` / `PUT /api/topics/{topic_id}` 返回最新 `Topic` 对象；后端暂无名称重复校验，需调用方确保唯一性。
- `DELETE /api/topics/{topic_id}` 成功时返回 `{ "success": true }`。

#### 3.4.2 主题数据接口
```
GET /api/topics/{topic_id}/events
获取主题匹配的事件列表
Query Parameters:
  - page: 页码
  - page_size: 页面大小
  - start_time: 开始时间
  - end_time: 结束时间
  - search: 搜索关键词

GET /api/topics/{topic_id}/stats
获取主题统计数据
Query Parameters:
  - start_time: 开始时间
  - end_time: 结束时间
```

- `GET /api/topics/{topic_id}/events` 返回 `PaginatedResponse<EventResponse>`，支持 `page`/`page_size` 分页；搜索字段会同时匹配事件编号、描述、处置结果。
- `GET /api/topics/{topic_id}/stats` 返回 `TopicStatsResponse`，仅包含按日统计，月度视图由前端聚合生成。

## 4. 业务逻辑

### 4.1 主题筛选流程

#### 4.1.1 筛选步骤
1. **数据预处理**：加载原始事件数据，处理空值和格式
2. **时间筛选**：根据指定时间范围过滤事件
3. **自由搜索**：根据搜索关键词在多字段中匹配
4. **包含关键词筛选**：应用第一道筛选条件
5. **排除关键词筛选**：应用第二道筛选条件
6. **精筛处理**：应用第四道筛选条件（可选）
7. **去重处理**：根据配置进行数据去重
8. **分类筛选**：应用分类配置的额外筛选条件

#### 4.1.2 筛选逻辑示例
```
原始事件：1000条
↓ 时间筛选（2025年1月）
剩余事件：800条
↓ 包含关键词筛选（噪音 AND 已处理）
剩余事件：150条
↓ 排除关键词筛选（排除KTV相关）
剩余事件：130条
↓ 去重处理（按事件描述）
最终事件：120条
```

### 4.2 统计计算逻辑

#### 4.2.1 日统计计算
- **数据聚合**：将事件按上报日期分组统计
- **移动平均**：计算7日移动平均值
- **异动检测**：基于全局均值和标准差判断异动

#### 4.2.2 月统计计算
- **数据聚合**：将日统计数据按月汇总
- **移动平均**：计算3月移动平均值
- **异动检测**：基于月度数据的统计特征判断异动

#### 4.2.3 异动计算公式
```
异动阈值 = 全局均值 + 2 × 标准差
异动判断 = 当期数值 > 异动阈值
```

### 4.3 数据一致性保证

#### 4.3.1 数据同步机制
- **实时更新**：主题配置变更后立即应用到筛选逻辑
- **缓存管理**：合理使用缓存提高查询性能
- **版本控制**：支持主题配置的版本管理和回滚

#### 4.3.2 错误处理
- **异常捕获**：完善的异常处理和错误提示
- **数据校验**：输入数据的格式和范围校验
- **降级策略**：在部分功能异常时的降级处理

## 5. 非功能性需求

### 5.1 性能要求
- **响应时间**：页面加载时间 < 3秒
- **并发处理**：支持50个并发用户同时使用
- **数据处理**：支持10万条事件数据的实时筛选
- **统计计算**：复杂统计查询响应时间 < 5秒

### 5.2 可用性要求
- **系统可用性**：99.5%以上
- **错误恢复**：在系统异常后能够快速恢复服务
- **数据备份**：重要配置数据定期备份

### 5.3 用户体验要求
- **界面友好**：简洁直观的用户界面
- **操作便捷**：减少用户操作步骤
- **反馈及时**：操作结果的及时反馈

### 5.4 兼容性要求
- **浏览器兼容**：支持Chrome、Firefox、Safari、Edge最新版本
- **设备兼容**：支持PC端和平板设备
- **数据兼容**：向前兼容旧版本的数据格式

## 6. 测试验收标准

### 6.1 功能测试
- **主题创建**：验证主题创建的完整流程和数据保存
- **关键词匹配**：验证各种关键词组合的匹配准确性
- **筛选功能**：验证时间、关键词、分类等筛选功能
- **统计计算**：验证统计数据的准确性和异动检测

### 6.2 性能测试
- **压力测试**：验证系统在高并发下的性能表现
- **负载测试**：验证大数据量下的处理能力
- **响应时间测试**：验证各功能模块的响应时间

### 6.3 兼容性测试
- **浏览器测试**：在不同浏览器中验证功能完整性
- **设备测试**：在不同分辨率设备上验证界面适配
- **数据格式测试**：验证新旧数据格式的兼容性

### 6.4 安全测试
- **输入验证**：验证用户输入的安全性检查
- **权限控制**：验证不同用户角色的权限控制
- **数据保护**：验证敏感数据的保护措施

## 7. 部署运维

### 7.1 部署环境
- **前端部署**：静态文件部署到Web服务器
- **后端部署**：Python应用部署到应用服务器
- **数据存储**：JSON文件存储或数据库部署

### 7.2 监控告警
- **系统监控**：监控系统运行状态和性能指标
- **业务监控**：监控主题数据更新和异动情况
- **错误告警**：及时发现和通知系统异常

### 7.3 备份恢复
- **数据备份**：定期备份主题配置和统计数据
- **灾难恢复**：制定完整的灾难恢复预案
- **版本回滚**：支持系统版本的快速回滚

## 8. 扩展规划

### 8.1 功能扩展
- **智能推荐**：基于历史数据推荐关键词配置
- **模板管理**：预设主题模板提高创建效率
- **协同编辑**：支持多用户协同编辑主题配置
- **API开放**：提供开放API供第三方系统集成

### 8.2 技术升级
- **数据库迁移**：从文件存储迁移到关系数据库
- **微服务架构**：拆分为独立的微服务模块
- **实时计算**：引入流式计算提升实时性
- **机器学习**：集成ML模型提升异动检测准确性

### 8.3 业务扩展
- **多租户支持**：支持多个组织独立使用
- **移动端适配**：开发专门的移动端应用
- **国际化支持**：支持多语言界面
- **数据可视化**：更丰富的图表和可视化组件
## 9. 实现评估与优化建议

| 领域 | 现状评估 | 建议优化 |
| --- | --- | --- |
| 主题创建校验 | 前端限制名称/描述长度，但后端缺少唯一性与长度校验，topics.json 可写入重复名称 | 在 `create_topic` / `update_topic` 增加服务端校验，返回 409/422；同步限制描述长度 |
| 去重选项 | 服务端支持 `description`/`event_id`/`null`，前端仅暴露描述去重开关 | 在 UI 中补充“按事件ID去重”“保留全部”选项，或在成功提示文案中说明可通过 API 调整 |
| 精筛关键词 | 模型支持 `fine_filters`，目前创建页默认传空数组且无输入项 | 视业务需要恢复精筛输入，或在规格中标注为“API 可用，UI 待实现” |
| 主题列表关键词展示 | 支持新旧数据结构，能够迁移 legacy 数组 | 保持现状，后续迁移完成后可考虑统一存储格式并清理 `keywords` 冗余字段 |
| 主题详情筛选 | 时间与搜索走服务端，列头筛选在前端执行 | 若数据规模增大，可研发服务端列头筛选 API，或在文档中强调其为客户端过滤避免误解 |
| 统计视图 | 后端仅返回按日数据，按月统计由前端聚合并计算 3 月均值 | 若需要统一口径，可在后端提供 `/stats/monthly` 接口，减少前端重复实现 |
| API 使用方式 | 前端创建/查询使用 `fetch`，未复用 `eventAPI` 封装 | 代码层面可后续重构为统一 API 模块，文档侧已在本节说明 base URL 与返回结构 |

---

*本需求规格说明书由系统分析得出，涵盖了事件主题管理系统的完整功能需求、技术实现和业务逻辑。如有疑问或需要补充，请及时沟通确认。*
