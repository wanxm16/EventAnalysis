
# 可编辑月报（指标占位符）Demo 需求说明（给 Codex）

> 版本：v1.0
> 撰写人：—
> 最后更新：2025-09-16
> 目标读者：前端、后端、测试、产品

---

## 0. TL;DR（一句话）

做一个 **所见即所得的在线报告编辑器** ，支持在正文里插入 **指标占位符** ，按选定月份自动取数； **发布时固化为快照** ； **一键导出 Word** 。完成 1 个标准模板的演示（海曙区 2025 年 8 月月报）。

---

## 1. 背景与目标

* 业务方每月出“矛盾纠纷与重点事件”报告，内容 70% 为固定结构 + 30% 为文字案例。
* 痛点：指标手填、环比计算易错；历史报告会随口径变化“漂移”。
* 目标：
  1. 在线编辑；2) 指标占位符自动取数（按月）；3) 发布固化；4) 导出 Word；5) 基础权限与审计。

> **说明** ：本 Demo 以“海曙区 2025 年 8 月报告”为样例，上线后仅需改月份即可生成 9 月版。

---

## 2. Demo 范围（Scope）

**在 Demo 中必须实现：**

1. 报告列表页（草稿/已发布查询、创建草稿）。
2. 报告编辑页（富文本、插入指标、刷新取数预览）。
3. 报告发布（固化快照，锁定数字）。
4. Word 导出（.docx）。
5. 指标库查询与取数（支持** **`@month` 参数，最少 10 个指标）。
6. 占位符 DSL 解析与渲染（见 §7）。

**可选（若工期允许）：**

* 动态表格占位符（附件 1/2 数据表）与简单柱状图占位符；
* 环比方向文案（上升/下降/持平）。

**明确不做（本期 Out of Scope）：**

* 多人协同在线编辑、全文版本 diff；
* 富图表样式定制、PPT 导出；
* 指标管理后台（用 Mock/配置文件替代）。

---

## 3. 角色与权限

* **作者** ：新建/编辑草稿、插入指标、刷新取数、预览、提交发布、导出。
* **审核/发布者** ：审核通过与发布。（Demo 可合并为一个角色）
* **阅读者** ：只读已发布报告与导出。

> Demo 简化：内置一个作者账号，一个发布者账号。

---

## 4. 关键用例（User Stories）

1. **创建 8 月报告** → 进入编辑器 → 在正文插入“重点事件数量”等指标 → 点击“刷新取数” → 预览出现“5314 件、环比 -5.2%” →** ****发布** → 内容固化。
2. **复制生成 9 月报告** → 选择月份** **`2025-09` → 仅更新数字 → 手工调整典型案例段落 → 发布。
3. **导出 Word** ：对已发布报告点击“导出 .docx”，下载统一样式文件。

---

## 5. 页面与流程

### 5.1 报告列表（/reports）

* 列表列：标题、月份、状态（草稿/已发布）、创建人、更新时间、操作。
* 操作：新建报告、继续编辑、预览、发布、导出。

### 5.2 编辑器（/reports/:id/edit）

* 富文本：标题、段落、加粗、列表、图片占位（可不实现上传）。
* **插入指标**按钮：弹窗搜索指标 → 选择月份（默认** **`@month`）与维度（默认“全区”）→ 插入占位符 Chip。
* **刷新取数** ：解析文档里的占位符 → 调用后端 → 将占位符在编辑器里显示实时值（预览态，不写库）。
* **发布** ：对所有占位符取数并 **固化为快照** ，写入** **`report_snapshot_values`，同时将正文中的占位符替换为数值文本。

### 5.3 预览（/reports/:id/preview）

* 与发布后展示相同样式，仅数据来自“临时展开”，不写快照。

---

## 6. 数据模型（简化）

```
indicators(
  id, code, name, unit, grain, desc, default_filters, provider, created_at, updated_at
)

indicator_values (可选预计算/Mock)
  indicator_id, period_month (YYYY-MM), scope, value, computed_at

report_templates (可选)
  id, name, content_md, style_meta, created_by

reports
  id, title, month(YYYY-MM), content_md_draft, status(enum: draft/published),
  created_by, created_at, updated_at, published_at

report_snapshot_values
  report_id, placeholder_id, indicator_code, period_month, scope, value, unit,
  precision, computed_at
```

> Demo 可不接真实库，**使用 provider=mock** 读取** **`indicator_values.json` 完成取数。

---

## 7. 占位符 DSL（必须）

统一格式：`[TYPE:标识|k1=v1|k2=v2]`

### 7.1 类型

* `KPI`：单值指标。
* `KPI_MOM_PCT`：环比百分比（上月对比）。
* `KPI_MOM_DIR`：环比方向（上升/下降/持平）。
* `TABLE`（可选）：表格数据集渲染。
* `CHART`（可选）：图表数据集渲染（仅生成静态图片 URL 占位）。

### 7.2 常用参数

* `period`：`@month` 或** **`YYYY-MM`。
* `scope`：`全区`/`镇街=南门` 等（Demo 只需** **`全区`）。
* `unit`：显示单位（例：件、%）。
* `precision`：小数位（默认 1）。
* `fmt`：`percent`/`number`（可选）。

### 7.3 示例

```
[KPI:重点事件数量|period=@month|scope=全区|unit=件]
[KPI_MOM_PCT:重点事件数量|period=@month|precision=1]
[KPI_MOM_DIR:重点事件数量|period=@month]
[TABLE:功能区受理情况表|dataset=受理情况明细|period=@month|cols=功能区,窗口,件数,人数]
```

> 解析规则：前端仅做语法高亮与 Chip 展示；实际取数由后端解析 DSL → 调用指标 provider。

---

## 8. 指标库（最少 10 项）

**必须包含：**

1. 重点事件数量（月，全区）
2. 退回核查闭环件数（月，全区）
3. 扬言极端类件数（月，全区）
4. 公共安全类追踪件数（月，全区）
5. 公共安全类-警网融合件数（月，全区）
6. 涉未成年人追踪件数（月，全区）
7. 涉未成年人-警网融合件数（月，全区）
8. 涉未成年人-非警务分流件数（月，全区）
9. 矛纠治安信访类追踪件数（月，全区）
10. 矛纠治安信访类-非警务分流件数（月，全区）
11. 矛纠治安信访类-警网融合件数（月，全区）
12. 矛纠治安信访类-退回件数（月，全区）

> Demo 数据建议：提供 2025-07 与 2025-08 两个月份的数值，用于演示环比。

---

## 9. 取数策略

* Demo 使用** ** **Mock Provider** ：`GET /api/indicators/value?code=xxx&period=YYYY-MM&scope=全区` → 直接返回固定值。
* 若接 KingBase：
  * SQL 在 provider 层配置，不在前端暴露；
  * 所有月度查询增加** **`dt` 或** **`report_time` 的月过滤；
  * 环比=** **`(本月-上月)/上月`，上月无数据 → 返回** **`null`。

---

## 10. API 设计（REST）

### 10.1 指标检索

`GET /api/indicators?keyword=重点事件&page=1&page_size=10`

```json
{
  "items": [{"code":"KPI_EVT_MAJOR","name":"重点事件数量","unit":"件","grain":"month"}],
  "total": 1
}
```

### 10.2 指标取值（Mock）

`GET /api/indicators/value?code=KPI_EVT_MAJOR&period=2025-08&scope=全区`

```json
{"code":"KPI_EVT_MAJOR","period":"2025-08","scope":"全区","value":5314,"unit":"件","precision":0}
```

### 10.3 占位符批量展开（编辑器刷新）

`POST /api/reports/:id/preview`

```json
{
  "content_md": "二、... [KPI:重点事件数量|period=@month|scope=全区|unit=件] ...",
  "month": "2025-08"
}
```

 **响应** （服务端返回“渲染后的 HTML 或 MD+值映射”）：

```json
{
  "rendered_html": "二、... 5314 件 ...",
  "values": [
    {"placeholder_id":"ph_1","code":"KPI_EVT_MAJOR","period":"2025-08","value":5314,"unit":"件"},
    {"placeholder_id":"ph_2","code":"KPI_EVT_MAJOR_MOM","period":"2025-08","value":-5.2,"unit":"%"}
  ]
}
```

### 10.4 发布（固化快照）

`POST /api/reports/:id/publish`

```json
{"month":"2025-08"}
```

 **响应** ：`200 OK`（写入** **`report_snapshot_values` 并返回已替换数值的 HTML）。

### 10.5 导出 Word

`GET /api/reports/:id/export?format=docx` → 返回文件流。

---

## 11. 前端交互细节

* 编辑器：TipTap/ProseMirror（推荐）或 Quill。
* 占位符 Chip：显示名称与参数（如「重点事件数量｜@month｜全区」）。Hover 显示：单位、上次取数时间、当前预览值。
* 刷新取数：对文档里所有占位符做一次批量解析与请求，渲染临时值（不写库）。
* 发布：弹窗确认 → 服务器替换文本并固化快照 → 状态变“已发布”。
* 导出：仅允许已发布状态导出（避免导出半成品）。

---

## 12. Word 导出实现建议

* 方案 A（推荐，快）：**HTML→DOCX** 转换（容器内装 LibreOffice/Pandoc 或使用 Node/Python 库）。
* 方案 B：`docx`/`docx4j`/`python-docx` 拼装（样式由 .dotx 模板提供）。
* 样式基线：
  * 标题：二号黑体，居中；
  * 小标题：三号黑体；
  * 正文：小四宋体，1.5 倍行距；
  * 表格：统一表头灰填充，内边距 6pt。

---

## 13. 错误处理与边界

* 指标无数据：显示“—”（em dash）并在 Hover 提示“上月/本月缺数据”。
* 占位符解析失败：Chip 标红，提供“查看原文/删除”操作。
* 发布时任一占位符取数失败：整体失败并返回失败清单。
* 并发：发布接口加幂等（根据 report_id+version）。

---

## 14. 日志与审计

* 记录操作：创建、编辑、刷新取数、发布、导出；
* 快照记录包含：indicator_code、period、scope、value、computed_at、data_version。

---

## 15. 安全

* 指标库的** ** **SQL/底表不可见** ；前端只看到名称/描述/单位；
* Demo 环境使用 Mock 数据，不暴露真实库连接。

---

## 16. 验收标准（Acceptance Criteria）

1. 在编辑器中插入以下占位符，点击刷新后出现预期数值：
   * `[KPI:重点事件数量|period=@month|scope=全区|unit=件]` →** ** **5314 件** （当** **`@month=2025-08`）。
   * `[KPI_MOM_PCT:重点事件数量|period=@month]` →** ** **-5.2%** 。
   * `[KPI:退回核查闭环件数|period=@month|unit=件]` →** ** **68 件** 。
   * `[KPI_MOM_PCT:退回核查闭环件数|period=@month]` →** ** **+112.5%** 。
   * `[KPI:扬言极端类件数|period=@month|unit=件]` →** ** **9 件** ；`[KPI_MOM_PCT:扬言极端类件数|period=@month]`→** ** **-18.0%** 。
2. 点击“发布”后，报告正文中的占位符均替换为 **静态数值** ，再次进入编辑页不可再被刷新改变。
3. 点击“导出 Word”下载的文件与预览内容一致（样式符合 §12）。

---

## 17. 模板示例（从业务原文抽象）

> 选择** **`@month=2025-08` 时应渲染出 5314/−5.2%/68/+112.5%/9/−18% 等数值。

```
**宁波市海曙区社会治理中心**

[@month]全区矛盾纠纷等重点事件追踪报告

（[@month]）

8月以来，区社会治理中心不断强化线上线下事件实质闭环，现将该月情况报告如下。

一、线下矛调功能区工作情况

略

二、线上平台重点事件闭环情况

区社治中心追踪监测“141”平台重点事件 [KPI:重点事件数量|period=@month|scope=全区|unit=件]，
环比 [KPI_MOM_DIR:重点事件数量|period=@month] [KPI_MOM_PCT:重点事件数量|period=@month]，
退回核查闭环 [KPI:退回核查闭环件数|period=@month|scope=全区|unit=件]，
环比 [KPI_MOM_DIR:退回核查闭环件数|period=@month] [KPI_MOM_PCT:退回核查闭环件数|period=@month]，
其中扬言、极端类事件 [KPI:扬言极端类件数|period=@month|scope=全区|unit=件]，
环比 [KPI_MOM_DIR:扬言极端类件数|period=@month] [KPI_MOM_PCT:扬言极端类件数|period=@month]。

（一）重点核查情况

1. 公共安全类事件。追踪监测 [KPI:公共安全类追踪件数|period=@month|unit=件]，
环比 [KPI_MOM_PCT:公共安全类追踪件数|period=@month]，其中警网融合事件 [KPI:公共安全类-警网融合件数|period=@month|unit=件]。

2. 涉未成年人类事件。追踪监测 [KPI:涉未成年人追踪件数|period=@month|unit=件]，
环比 [KPI_MOM_PCT:涉未成年人追踪件数|period=@month]，其中警网融合 [KPI:涉未成年人-警网融合件数|period=@month|unit=件]、
非警务分流 [KPI:涉未成年人-非警务分流件数|period=@month|unit=件]。

3. 矛盾纠纷类事件。追踪监测 [KPI:矛纠治安信访类追踪件数|period=@month|unit=件]，
环比 [KPI_MOM_PCT:矛纠治安信访类追踪件数|period=@month]，其中非警务分流 [KPI:矛纠治安信访类-非警务分流件数|period=@month|unit=件]、
警网融合 [KPI:矛纠治安信访类-警网融合件数|period=@month|unit=件]，退回 [KPI:矛纠治安信访类-退回件数|period=@month|unit=件]。

（二）退回事件分析

1. 退回类型分析。区中心退回 [KPI:退回件数|period=@month|unit=件]，其中阶段性闭环 [KPI:退回-阶段性闭环件数|period=@month|unit=件]
（占比 [KPI:退回-阶段性闭环占比|period=@month|unit=%|precision=1]），过简闭环 [KPI:退回-过简闭环件数|period=@month|unit=件]，错误办理 [KPI:退回-错误办理件数|period=@month|unit=件]。

（三）典型闭环事件

（人工编辑段落，不做占位符约束）

附件 1：功能区受理情况表

[TABLE:功能区受理情况表|dataset=受理情况明细|period=@month|cols=功能区,窗口,件数,人数]

附件 2：全市闭环处置清单平台事件处置情况

[TABLE:市平台事件处置清单|dataset=市平台井盖问题清单|period=@month|cols=序号,事件描述,所属镇街,事权单位,闭环内容,闭环时间]
```

---

## 18. Mock 数据（示例）

### 18.1** **`indicator_values.json`

```json
{
  "2025-07": {
    "KPI_EVT_MAJOR": 5605,
    "KPI_EVT_RET_CLOSED": 32,
    "KPI_EVT_EXTREME": 11
  },
  "2025-08": {
    "KPI_EVT_MAJOR": 5314,
    "KPI_EVT_RET_CLOSED": 68,
    "KPI_EVT_EXTREME": 9
  }
}
```

> `KPI_MOM_PCT` 由后端计算：例如** **`(5314-5605)/5605 = -0.052 → -5.2%`；退回核查闭环** **`(68-32)/32 = +112.5%`；极端类** **`(9-11)/11 = -18.18% → -18.0%`（precision=1 四舍五入）。

### 18.2 代码表

```
KPI_EVT_MAJOR                 重点事件数量（月）
KPI_EVT_RET_CLOSED           退回核查闭环件数（月）
KPI_EVT_EXTREME              扬言极端类件数（月）
KPI_PUBSAFE_TRACK            公共安全类追踪件数（月）
KPI_PUBSAFE_MERGE            公共安全类-警网融合件数（月）
KPI_MINOR_TRACK              涉未成年人追踪件数（月）
KPI_MINOR_MERGE              涉未成年人-警网融合件数（月）
KPI_MINOR_NONPOLICE          涉未成年人-非警务分流件数（月）
KPI_CONFLICT_TRACK           矛纠治安信访类追踪件数（月）
KPI_CONFLICT_NONPOLICE       矛纠治安信访类-非警务分流件数（月）
KPI_CONFLICT_MERGE           矛纠治安信访类-警网融合件数（月）
KPI_CONFLICT_RETURN          矛纠治安信访类-退回件数（月）
```

---

## 19. 技术选型（建议）

* 前端：React + TipTap（占位符 Node/Mark 扩展）+ Tailwind（样式可简化）。
* 后端：Node（NestJS/Express）或 Python（FastAPI），选其一；
* 导出：HTML→DOCX（容器内安装 LibreOffice/或使用 docx 库）。
* 鉴权：Cookie Session 或简单 Token（Demo 可跳过 SSO）。

---

## 20. 开发清单（WBS）

1. 占位符解析器（前后端各一份，前端用于识别 Chip，后端用于实际计算）。
2. 指标 Provider（Mock + 环比计算）。
3. 报告 CRUD 与状态机（草稿/已发布）。
4. 编辑器集成（插入指标弹窗 + 刷新取数）。
5. 发布固化与快照表。
6. 导出 Word。
7. 日志与基础审计。
8. 验收用模板与 Mock 数据装载。

---

## 21. 测试清单（要点）

* 占位符语法：缺参数、未知 code、重复插入、空格/中英文冒号混用。
* 边界：上月无数据/本月无数据；环比分母为 0；precision 舍入。
* 并发发布：同一报告双击发布按钮应只成功一次。
* 导出与预览一致性：页面与 Word 中文字、表格一致。

---

## 22. 未来扩展（非必做）

* 数据集/图表占位符的自定义过滤与排序；
* 模板中心（一个模板生成多报告）；
* 指标管理后台与权限（口径审批/版本化）；
* PPT 导出与自动配图。

---

**附录 A：口径 SQL（KingBase/PG 内核，示意）**

```sql
-- 重点事件数量（月，全区）
-- 入参：p_start='2025-08-01'，p_end='2025-09-01'（左闭右开）
SELECT COUNT(*) AS val
FROM dwd_conflict_event_detail_df
WHERE event_level='重点'
  AND report_time >= DATE '2025-08-01'
  AND report_time <  DATE '2025-09-01';
```

（其他口径按业务条件添加 WHERE 子句即可，Demo 可不连库）

---

> 以上内容即可直接进入开发；若需要，我可以提供占位符解析的正则与示例代码片段。
>



以下是报告原文


**宁波市海曙区社会治理中心**

8月份全区矛盾纠纷等重点事件追踪报告

（2025年8月）

8月以来，区社会治理中心不断强化线上线下事件实质闭环，现将该月情况报告如下。

1. 线下矛调功能区工作情况

略

二、线上平台重点事件闭环情况

区社治中心追踪监测“141”平台重点事件5314件，环比下降5.2%，退回核查闭环68件，环比上升112.5%，其中扬言、极端类事件9件，环比下降18%。![img](ed12329fe0b223831d6c58e22ce5ba3b.jpeg)

（一）重点核查情况

1.公共安全类事件。追踪监测170件，环比下降8%，其中警网融合事件37件。

2.涉未成年人类事件。追踪监测涉未成年人事件145件，环比下降17.6%，其中警网融合11件、非警务分流48件。

3.矛盾纠纷类事件。追踪监测矛盾纠纷类、治安隐患类、信访维稳类事件4999件，环比下降4.5%，其中非警务分流 3894件、警网融合754件，退回68件。

（二）退回事件分析

1.退回类型分析。区中心退回68件主要为阶段性闭环38件，占比55.9%，过简闭环28件、错误办理2件。经督促，退回事件已重新闭环。

2.退回分布情况。集士港、南门、石碶退回较多，分别为11件、11件、10件，白云、鄞江、龙观工作较好，没有退回事件。

![](394e284c9ffede4c88dab63597179d78.jpeg)（三）典型闭环事件

1.阶段性闭环。8月14日公安110警情：当事人亲属在第一医院治疗用药后死亡，想找药厂方处理。

民警现场处置：引导家属到医患办联系厂家，厂方回复当事人明天派领导到宁波协商处理事宜。因当事人住在月湖辖区宾馆，公安现场处置后将该事件推送社区进行后续关注。

**初次办结意见：**综治工作人员已联系相关部门进一步跟进处理。**二次闭环意见：**经与医患办、属地民警了解，厂方领导对涉事家属送去悼念物品，在双方沟通下，报警人稳定情绪，达成一致，收下物品，纠纷化解。

2.过简闭环。8月20日公安110警情：章水镇一起邻里纠纷，当事人因历史建房纠纷，目前赤水村造房子可能会再次引发纠纷，希望镇、村两级密切关注。

**初次办结意见：**双方村民正在协商。**二次闭环意见：**村干部、网格员已与双方当事人进行沟通，了解双方诉求及情绪动态，上门做安抚工作，同时联系镇平安法治办、司法所提供法律咨询和支持，在处理本次潜在纠纷的同时，村干部也尝试同步化解其遗留历史隔阂，引导双方依法依规解决问题，共建和谐邻里关系。

三、全市涉我区重点事件闭环情况

本月，根据市委副书记张丽批示要求，市社会治理中心对平台未办结的存在安全问题的井盖破损缺失问题进行了梳理督办，涉我区此类未闭环事件共27件，截至目前，已全部闭环。

附件：1.区社会治理中心矛调功能区受理情况表

2.全市闭环处置清单平台事件处置情况

附件1

区社会治理中心矛调功能区受理情况表

| 功能区          | 窗口                 | 件数 | 人数 |
| --------------- | -------------------- | ---- | ---- |
| 信访接待区      | 信访                 | 85   | 270  |
|                 | 检察院               | 13   | 20   |
|                 | 公安                 | 19   | 29   |
|                 | 纪委监委（轮驻）     | 19   | 26   |
|                 | 人社（轮驻）         | 4    | 4    |
|                 | 住建（轮驻）         | 4    | 4    |
|                 | 农业农村（轮驻）     | 1    | 1    |
|                 | 综合行政执法（轮驻） | 1    | 1    |
|                 | 自然资源规划（轮驻） | 1    | 2    |
|                 | 房屋征收（轮驻）     | 1    | 3    |
|                 | 总工会（轮驻）       | 2    | 2    |
| 诉讼法律 服务区 | 诉讼                 | 983  | 1223 |
|                 | 法律咨询/法律援助    | 110  | 140  |
|                 | 行政复议             | 16   | 19   |
|                 | 公证                 | 1    | 1    |
| 矛盾纠纷 调解区 | 调解                 | 128  | 233  |
| **合计**  | 1388                 | 1978 |      |

附件2

全市闭环处置清单平台事件处置情况

| **序号** | **事件描述**                                             | **所属镇街** | **事权单位** | **闭环内容**               | **闭环时间** |
| -------------- | -------------------------------------------------------------- | ------------------ | ------------------ | -------------------------------- | ------------------ |
| 1              | 海曙区机场路鲍家1号门前电力井盖破损。                          | 石碶街道           | 石碶街道           | 已修复                           | 8.22               |
| 2              | 海曙区镇明路316号西北30米处污水井盖破损                        | 月湖街道           | 综合管线           | 已修复                           | 8.26               |
| 3              | 海曙区碶闸街16号门前污水井盖破损                               | 江厦街道           | 江厦街道           | 已修复                           | 8.21               |
| 4              | 海曙区药行街33号门前污水井盖凸起                               | 江厦街道           | 无主设施           | 经对接，由海欣控股代处理，已修复 | 9.5                |
| 5              | 海曙区和义路与药局巷交叉口北侧约10米有无主井盖破损             | 鼓楼街道           | 无主设施           | 经对接，由住建局代处理，已修复   | 8.27               |
| 6              | 海曙区永丰西路126弄92号西侧电力井盖铺板破损                    | 西门街道           | 西门街道           | 已修复                           | 8.26               |
| 7              | 海曙区永丰西路201号北侧共同沟井盖破损                          | 西门街道           | 西门街道           | 已修复                           | 8.25               |
| 8              | 海曙区广安路209弄15号南侧约10米无主井盖破洞立面杂物堵塞        | 望春街道           | 鼓楼街道           | 已清理                           | 8.21               |
| 9              | 海曙区碶闸街52号前面电力井盖破损                               | 江厦街道           | 江厦街道           | 及时维护，并消除安全隐患         | 8.23               |
| 10             | 海曙区柳庄街51号门前有综合井盖沉降                             | 西门街道           | 综合管线           | 已修复                           | 8.3                |
| 11             | 海曙区高塘路新水世界浴场对面有污水井盖周边破损                 | 西门街道           | 西门街道           | 已修复                           | 8.25               |
| 12             | 海曙区尹江路193号世纪华联店门口东侧井盖（无标识）板石破损      | 南门街道           | 南门街道           | 已修复                           | 9.3                |
| 13             | 海曙区机场路与姚江西路交叉口西北侧约10米通讯井盖铺砖破损       | 望春街道           | 海欣控股           | 已修复                           | 8.22               |
| 14             | 海曙区鄞县大道西段洪渡新村商住楼3号门前雨水井盖周边破损。      | 石碶街道           | 海欣控股           | 已修复                           | 8.2                |
| 15             | 海曙区徐家漕路145号前有污水井盖周边板石破损                    | 望春街道           | 海欣控股           | 已修复                           | 8.22               |
| 16             | 海曙区和义路与药局巷交叉口东侧约20米共同沟井盖周边破损         | 鼓楼街道           | 鼓楼街道           | 已修复                           | 8.25               |
| 17             | 海曙区徐家漕路107号朝东十五米有电力井盖上板石铺装破损          | 望春街道           | 海欣控股           | 已修复                           | 8.22               |
| 18             | 海曙区西河街107-1号北侧20米处有电力井盖破损                    | 西门街道           | 海欣控股           | 已修复                           | 8.25               |
| 19             | 海曙区尚书街65号前面无主井盖破损                               | 鼓楼街道           | 海欣控股           | 已修复                           | 8.25               |
| 20             | 海曙区新芝路与新高路交叉口东南侧有上水井盖周边破损             | 西门街道           | 海欣控股           | 已修复                           | 8.25               |
| 21             | 海曙区县前街与药局巷交叉口西侧有污水井盖沉降                   | 鼓楼街道           | 鼓楼街道           | 已修复                           | 8.25               |
| 22             | 海曙区新星路与悠云路交叉口西北侧约20米交警井盖周边破损凸起     | 望春街道           | 交警大队           | 已修复                           | 8.21               |
| 23             | 海曙区雅戈尔大道435号门前电力井盖破损。                        | 石碶街道           | 石碶街道           | 已修复                           | 8.19               |
| 24             | 海曙区雅戈尔大道489号门前车行道上综合井盖沉降。                | 石碶街道           | 无主设施           | 经对接，由华数代处理，已修复     | 8.30               |
| 25             | 海曙区马园路与柳汀街交叉口西北侧约10米有综合井盖周边破损       | 南门街道           | 移动公司           | 已修复                           | 8.2                |
| 26             | 海曙区苗圃路与马园路交叉口电信井盖周边破损弹跳                 | 南门街道           | 电信公司           | 已修复                           | 8.19               |
| 27             | 海曙区新星路与机场路交叉口东北侧约60米上水井盖周边铺砖开裂松动 | 望春街道           | 区住建局           | 已修复                           | 8.2                |

# 可编辑月报（指标占位符）Demo 需求说明（实现记录）

本文件对应需求文档的落地实现记录。本次提交已完成后端 API 与最小数据层（Mock）的搭建，并在“统计报告”页补充了月度统计分析模块。后续可继续补充前端“报告列表/编辑/发布/导出”的页面。

## 已实现（后端 FastAPI）

- 指标目录与取值（Mock Provider）
  - `GET /api/indicators?keyword=...`：关键字检索，返回 code/name/unit
  - `GET /api/indicators/value?code=...&period=YYYY-MM&scope=全区`：从 `data/indicator_values.json` 返回固定值
  - 指标目录内置 ≥10 项（见 data/indicator_values.json 与 services 初始化）

- 报告 CRUD 与预览/发布
  - `GET /api/reports`：报告列表
  - `POST /api/reports`：创建草稿（title、month、content_md_draft）
  - `GET /api/reports/{id}`：获取单个
  - `PUT /api/reports/{id}`：更新草稿
  - `POST /api/reports/{id}/preview`：解析正文中的占位符（DSL）并返回渲染后的 HTML 与占位符值清单
  - `POST /api/reports/{id}/publish`：对草稿进行取数并固化快照（写入 data/report_snapshot_values.json），报告状态置为 published
  - `GET /api/reports/{id}/export?format=docx`：将渲染后的文本导出为 Docx（简易文本导出，已添加 python-docx 依赖）

- 占位符 DSL（最简版解析）
  - 语法：`[TYPE:名称|k=v|k=v]`，支持：`KPI`、`KPI_MOM_PCT`、`KPI_MOM_DIR`
  - 参数：`period=@month|YYYY-MM`、`scope`、`unit`、`precision`
  - 环比：`(本月-上月)/上月`，上月或分母缺失时返回 `—`

## 已实现（统计报告页补充）

- 新增“📈 月度统计分析”卡片：
  - 过滤：按月份区间（按月）、镇街（多选）、事件级别（多选）
  - 折线图：月总量趋势（倒序取最近 30 点）
  - 堆叠柱图：按镇街、按级别的月度分布
  - 后端接口：`GET /api/statistics/monthly`

## 数据文件

- `data/indicator_values.json`：Demo 指标 2025-07/08 的月度值
- `data/reports.json`：报告元数据存储（自动生成）
- `data/report_snapshot_values.json`：发布时快照值（自动生成）

## 待实现（下一步）

- 前端“报告列表/编辑/预览/发布/导出”页面：
  - 编辑器可初期用 Markdown + 占位符 Chip 显示；后续可换 TipTap
  - 插入指标弹窗（检索指标、选择 period=@month、scope=全区、precision）
  - 预览=调用 `/api/reports/{id}/preview`
  - 发布=调用 `/api/reports/{id}/publish`（固化）
  - 导出=调用 `/api/reports/{id}/export?format=docx`

- 导出样式：按 §12 要求补充样式模板（可引入 HTML→DOCX 更优方案）

---

注：完整需求原文见项目对话记录；本文件将随着实现进展同步更新。
