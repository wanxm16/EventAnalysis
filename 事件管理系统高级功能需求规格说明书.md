# 事件管理系统高级功能需求规格说明书

## 1. 概述

### 1.1 功能描述
本文档描述了事件分析系统的高级功能需求，包括智能搜索、高级筛选、数据排序、表格定制化等功能。这些功能旨在提升用户体验，提高数据检索效率，满足不同用户的个性化需求。

### 1.2 业务价值
- **提高搜索精度**：通过包含排除逻辑减少无关结果
- **增强筛选能力**：支持多维度、多选项的复杂筛选
- **优化数据展示**：支持排序、列管理等个性化展示
- **提升用户体验**：直观的操作界面和灵活的定制功能
- **提高工作效率**：快速定位目标数据，减少查找时间

## 2. 核心功能需求

### 2.1 智能搜索功能

#### 2.1.1 包含排除搜索
- **包含关键词逻辑（OR关系）**
  - 当设置多个包含关键词时，结果只需包含任意一个关键词即可
  - 搜索不区分大小写
  - 支持部分匹配（子字符串匹配）
  - 示例：搜索["离婚", "酒店"]，返回包含"离婚"**或**"酒店"的事件

- **排除关键词逻辑（OR关系）**
  - 当设置多个排除关键词时，结果不能包含任何一个排除关键词
  - 搜索不区分大小写
  - 支持部分匹配（子字符串匹配）
  - 示例：排除["交通", "群体"]，去除包含"交通"**或**"群体"的事件

- **组合搜索逻辑**
  - 包含和排除条件同时存在时的逻辑关系
  - 最终逻辑：`(包含词1 OR 包含词2 OR ...) AND NOT (排除词1 OR 排除词2 OR ...)`

#### 2.1.2 多字段独立搜索
- **事件描述字段（search_desc）**
  - API参数：`include_desc`, `exclude_desc`
  - 搜索范围：仅限事件描述字段内容
  - 前端显示：在"事件描述"列的筛选器中

- **处置结果字段（search_result）**
  - API参数：`include_result`, `exclude_result`
  - 搜索范围：仅限处置结果字段内容
  - 前端显示：在"处置结果"列的筛选器中

- **通用搜索（向后兼容）**
  - API参数：`include`, `exclude`
  - 搜索范围：事件编号、事件描述、处置结果、报警人信息的组合字段
  - 前端显示：在"事件编号"列的简单搜索框中

### 2.2 高级筛选功能

#### 2.2.1 多选筛选支持
- **镇街名称筛选**
  - 支持多个镇街同时选择
  - 筛选逻辑：OR关系（满足任一条件即可）
  - 界面：下拉多选框，支持搜索和清除

- **事件级别筛选**
  - 支持多个级别同时选择（一级事件、二级事件、三级事件等）
  - 筛选逻辑：OR关系
  - 视觉效果：不同级别用不同颜色标签区分

- **二级分类筛选**
  - 支持多个分类同时选择
  - 筛选逻辑：OR关系
  - 动态加载：根据数据动态生成分类选项

- **村社名称筛选**
  - 支持多个村社同时选择
  - 联动筛选：可与镇街筛选联动
  - 界面：下拉多选框，支持模糊搜索

#### 2.2.2 相关事件数量筛选
- **筛选选项**
  - 无关联事件（0个）
  - 1个关联事件
  - 2-5个关联事件
  - 5个以上关联事件
- **单选逻辑**：只能选择一个数量范围
- **数据源**：基于聚类分析结果

#### 2.2.3 时间范围筛选
- **日期选择器**：支持开始时间和结束时间选择
- **时间格式**：支持日期时间选择（YYYY-MM-DD HH:mm:ss）
- **灵活筛选**：支持只设置开始时间或结束时间
- **特殊格式处理**：兼容多种时间格式（如"5/1/25 8:20"）

### 2.3 数据排序功能

#### 2.3.1 服务器端排序
- **全数据排序**：对所有数据进行排序，不仅限于当前页
- **排序字段**：支持多个字段的排序功能
- **排序方向**：升序（ASC）和降序（DESC）
- **默认排序**：按上报时间倒序排列

#### 2.3.2 时间字段排序
- **智能解析**：支持多种时间格式的解析和比较
- **排序准确性**：基于时间戳进行精确排序
- **异常处理**：无效时间格式的容错处理

#### 2.3.3 排序状态维护
- **视觉反馈**：列标题显示排序方向指示器
- **状态保持**：排序状态在分页切换时保持
- **多字段支持**：未来可扩展支持多字段组合排序

### 2.4 表格定制化功能

#### 2.4.1 列宽度拖拽调整
- **实时调整**：鼠标拖拽即时调整列宽度
- **最小宽度限制**：防止列宽过小影响显示（最小50px）
- **拖拽区域**：列标题右侧5-10px区域作为拖拽热区
- **光标提示**：悬停时显示resize光标
- **状态保存**：调整后的宽度自动保存

#### 2.4.2 自定义列显示
- **列选择器**：可选择显示或隐藏特定列
- **默认配置**：预设常用列的显示配置
- **保存设置**：用户选择保存到本地存储
- **恢复默认**：一键恢复到系统默认配置

#### 2.4.3 列顺序自定义
- **拖拽排序**：通过拖拽调整列的显示顺序
- **固定列**：事件编号（首列）和操作列（末列）位置固定
- **可视化反馈**：拖拽过程中的视觉指示
- **设置保存**：自定义顺序保存到本地存储

### 2.5 筛选条件管理

#### 2.5.1 筛选条件展示
- **标签显示**：当前生效的筛选条件以彩色标签形式展示
- **分类标识**：不同类型筛选条件使用不同颜色区分
  - 描述关键词：蓝色（blue）
  - 处置关键词：深蓝色（geekblue）
  - 镇街筛选：绿色（green）
  - 事件级别：橙色（orange）
  - 二级分类：紫色（purple）
  - 相关事件：青色（cyan）
  - 时间筛选：火山红（volcano）

#### 2.5.2 筛选条件操作
- **单独删除**：点击标签关闭按钮删除单个筛选条件
- **批量清除**：一键清除所有筛选条件
- **条件组合**：支持多个筛选条件的组合使用

## 3. 用户界面设计

### 3.1 搜索界面
- **搜索筛选框设计**
  - 包含关键词输入区域：支持多个关键词，回车添加
  - 排除关键词输入区域：支持多个关键词，回车添加
  - 提示文本："包含关键词（回车添加）" / "排除关键词（回车添加）"
  - 示例："如：离婚 酒店" / "如：交通 群体"

### 3.2 筛选界面
- **多选筛选器**
  - 下拉多选框界面
  - 支持全选/反选功能
  - 搜索过滤功能
  - 已选项目计数显示

### 3.3 表格界面
- **列标题增强**
  - 排序指示器（升序/降序箭头）
  - 筛选状态指示器（蓝色图标）
  - 拖拽调整区域提示

- **操作按钮区域**
  - 自定义列按钮：表格右上角
  - 清除筛选按钮：筛选条件区域
  - 刷新数据按钮：页面顶部

### 3.4 视觉反馈
- **筛选条件标签**：不同颜色标识不同类型
- **排序状态显示**：列标题箭头指示器
- **拖拽光标**：列宽调整时的resize光标
- **加载状态**：数据加载时的loading指示器

## 4. 技术实现

### 4.1 前端技术架构

#### 4.1.1 状态管理
```javascript
// 筛选状态
const [searchParams, setSearchParams] = useState({});
const [columnFilters, setColumnFilters] = useState({});

// 排序状态
const [sortField, setSortField] = useState(null);
const [sortOrder, setSortOrder] = useState(null);

// 列管理状态
const [visibleColumnKeys, setVisibleColumnKeys] = useState([]);
const [columnWidths, setColumnWidths] = useState({});
```

#### 4.1.2 搜索组件实现
```javascript
const getColumnSearchProps = (dataIndex, placeholder, apiParam) => ({
  filterDropdown: ({ setSelectedKeys, selectedKeys, confirm, clearFilters }) => (
    <div style={{ padding: 8, width: 300 }}>
      {/* 包含关键词 */}
      <div>包含关键词（回车添加）</div>
      <Select mode="tags" value={state.include} onChange={updateInclude} />
      
      {/* 排除关键词 */}
      <div>排除关键词（回车添加）</div>
      <Select mode="tags" value={state.exclude} onChange={updateExclude} />
      
      {/* 操作按钮 */}
      <Button onClick={handleSearch}>搜索</Button>
      <Button onClick={handleReset}>重置</Button>
    </div>
  )
});
```

#### 4.1.3 列宽拖拽组件
```javascript
const ResizeableTitle = (props) => {
  const { onResize, width, ...restProps } = props;
  
  return (
    <th style={{ position: 'relative' }}>
      {restProps.children}
      <div
        style={{ cursor: 'col-resize' }}
        onMouseDown={handleMouseDown}
      />
    </th>
  );
};
```

### 4.2 后端技术实现

#### 4.2.1 API接口设计
```python
def get_events(
    page: int = 1, 
    page_size: int = 20,
    # 搜索参数
    include_desc: Optional[List[str]] = None,
    exclude_desc: Optional[List[str]] = None,
    include_result: Optional[List[str]] = None,
    exclude_result: Optional[List[str]] = None,
    # 筛选参数
    town: Optional[List[str]] = None,
    level: Optional[List[str]] = None,
    category: Optional[List[str]] = None,
    # 排序参数
    sort_field: Optional[str] = None,
    sort_order: Optional[str] = None
) -> PaginatedResponse:
```

#### 4.2.2 搜索逻辑实现
```python
# 包含关键词：OR关系（包含任何一个即可）
if include_desc:
    include_condition = None
    for kw in include_desc:
        condition = df['事件描述'].str.contains(re.escape(kw), na=False)
        if include_condition is None:
            include_condition = condition
        else:
            include_condition = include_condition | condition
    df = df[include_condition]

# 排除关键词：OR关系（不能包含任何一个）
if exclude_desc:
    for kw in exclude_desc:
        df = df[~df['事件描述'].str.contains(re.escape(kw), na=False)]
```

#### 4.2.3 排序逻辑实现
```python
# 动态排序处理
ascending = True if sort_order == 'asc' else False

if sort_field == '上报时间':
    # 时间字段特殊处理
    df['上报时间_parsed'] = pd.to_datetime(df['上报时间'], errors='coerce')
    df = df.sort_values('上报时间_parsed', ascending=ascending, na_position='last')
else:
    # 其他字段直接排序
    df = df.sort_values(sort_field, ascending=ascending, na_position='last')
```

### 4.3 安全考虑
- 使用`re.escape()`对用户输入进行转义，防止正则表达式注入
- 输入长度限制和格式验证
- SQL注入防护（如适用）
- XSS防护处理

## 5. 性能优化

### 5.1 前端性能
- **防抖机制**：搜索输入防抖，避免频繁API调用
- **虚拟滚动**：大数据量表格的虚拟滚动优化
- **状态缓存**：搜索状态和用户配置的本地缓存
- **懒加载**：筛选选项的按需加载

### 5.2 后端性能
- **索引优化**：对常用搜索字段建立索引
- **分页优化**：高效的分页查询实现
- **缓存策略**：筛选选项数据的缓存
- **查询优化**：复杂筛选条件的查询优化

### 5.3 用户体验优化
- **加载状态**：数据加载过程的loading指示
- **错误处理**：友好的错误提示和降级处理
- **响应式设计**：适配不同屏幕尺寸
- **键盘操作**：支持常用的键盘快捷键

## 6. 兼容性要求

### 6.1 浏览器兼容性
- Chrome 70+
- Firefox 65+
- Safari 12+
- Edge 79+

### 6.2 向后兼容
- 保持原有简单搜索功能不变
- 支持原有的API参数格式
- 新功能不影响现有用户操作习惯

### 6.3 扩展性设计
- 搜索框架支持扩展到更多字段
- 筛选组件可复用于其他列
- 排序机制支持多字段排序扩展
- 列管理支持更多定制选项

## 7. 测试用例

### 7.1 搜索功能测试
- **基本搜索测试**
  - 单个包含关键词搜索
  - 单个排除关键词搜索
  - 多个包含关键词的OR逻辑验证
  - 多个排除关键词的OR逻辑验证
  - 包含排除关键词的组合逻辑验证

- **多字段搜索测试**
  - 事件描述字段独立搜索
  - 处置结果字段独立搜索
  - 多字段同时搜索的组合效果

### 7.2 筛选功能测试
- **多选筛选测试**
  - 镇街名称多选筛选
  - 事件级别多选筛选
  - 二级分类多选筛选
  - 多个筛选条件组合测试

- **时间筛选测试**
  - 开始时间筛选
  - 结束时间筛选
  - 时间范围筛选
  - 特殊时间格式处理

### 7.3 排序功能测试
- **排序逻辑测试**
  - 上报时间字段升序排序
  - 上报时间字段降序排序
  - 跨页排序验证
  - 排序状态保持测试

### 7.4 表格定制测试
- **列宽拖拽测试**
  - 各列宽度拖拽调整
  - 最小宽度限制验证
  - 拖拽状态保存测试

- **列管理测试**
  - 列显示隐藏功能
  - 列顺序拖拽调整
  - 配置保存和恢复

### 7.5 边界条件测试
- **输入边界测试**
  - 空关键词处理
  - 超长字符串处理
  - 特殊字符处理（正则表达式特殊字符）
  - 大小写不敏感测试

- **数据边界测试**
  - 空数据集处理
  - 大数据量处理
  - 网络异常处理

### 7.6 性能测试
- **响应时间测试**
  - 搜索响应时间 < 2秒
  - 筛选响应时间 < 1秒
  - 排序响应时间 < 3秒

- **并发测试**
  - 多用户同时搜索
  - 高并发筛选操作

## 8. 验收标准

### 8.1 功能验收

#### 8.1.1 搜索功能
- [ ] 支持事件描述字段的包含排除搜索
- [ ] 支持处置结果字段的包含排除搜索
- [ ] 包含关键词实现OR逻辑
- [ ] 排除关键词实现OR逻辑
- [ ] 组合搜索逻辑正确
- [ ] 搜索结果准确性验证

#### 8.1.2 筛选功能
- [ ] 镇街名称支持多选筛选
- [ ] 事件级别支持多选筛选
- [ ] 二级分类支持多选筛选
- [ ] 村社名称支持多选筛选
- [ ] 相关事件数量筛选正常
- [ ] 时间范围筛选功能正常
- [ ] 多个筛选条件组合使用正常

#### 8.1.3 排序功能
- [ ] 上报时间字段支持排序
- [ ] 排序对全部数据生效（非仅当前页）
- [ ] 升序降序切换正常
- [ ] 排序状态正确显示
- [ ] 排序与筛选组合使用正常

#### 8.1.4 表格定制功能
- [ ] 所有列支持宽度拖拽调整
- [ ] 列宽度调整实时生效
- [ ] 最小宽度限制生效
- [ ] 列显示隐藏功能正常
- [ ] 列顺序拖拽调整正常
- [ ] 自定义配置保存和恢复

### 8.2 界面验收
- [ ] 筛选条件以Tag形式正确显示
- [ ] 不同筛选类型使用不同颜色标识
- [ ] 支持单独删除筛选条件
- [ ] 提供清除全部筛选功能
- [ ] 筛选图标状态正确显示
- [ ] 排序指示器正确显示
- [ ] 拖拽光标正确显示

### 8.3 性能验收
- [ ] 搜索响应时间 < 2秒
- [ ] 筛选响应时间 < 1秒
- [ ] 排序响应时间 < 3秒
- [ ] 支持大数据集（10万+记录）操作
- [ ] 前端操作流畅无卡顿
- [ ] 内存使用合理

### 8.4 兼容性验收
- [ ] 原有搜索功能正常工作
- [ ] 与其他系统功能正确组合
- [ ] 主流浏览器兼容性测试通过
- [ ] 移动端适配验证
- [ ] 向后兼容性验证

### 8.5 用户体验验收
- [ ] 操作流程直观易懂
- [ ] 功能发现性良好
- [ ] 错误处理友好
- [ ] 加载状态提示清晰
- [ ] 帮助提示完整

## 9. 项目里程碑

### 9.1 阶段一：核心搜索功能（已完成）
- ✅ 包含排除关键词搜索
- ✅ 多字段独立搜索
- ✅ 搜索结果回显

### 9.2 阶段二：筛选和排序功能（已完成）
- ✅ 多选筛选支持
- ✅ 服务器端排序
- ✅ 时间字段排序

### 9.3 阶段三：表格定制功能（已完成）
- ✅ 列宽度拖拽调整
- ✅ 自定义列显示
- ✅ 列顺序自定义

### 9.4 阶段四：优化和完善
- [ ] 性能优化
- [ ] 用户体验优化
- [ ] 移动端适配
- [ ] 文档完善

## 10. 维护和升级

### 10.1 功能扩展规划
- **高级排序**：多字段组合排序
- **智能筛选**：基于用户行为的智能推荐
- **数据导出**：筛选结果的导出功能
- **视图保存**：用户自定义视图的保存和分享

### 10.2 性能监控
- **响应时间监控**：各功能模块的响应时间跟踪
- **用户行为分析**：功能使用频率统计
- **错误率监控**：异常情况的监控和告警

### 10.3 用户反馈机制
- **功能反馈收集**：用户对新功能的反馈收集
- **使用习惯分析**：基于用户操作数据的功能优化
- **需求收集**：用户新需求的收集和评估

---

*文档版本：v2.0*  
*最后更新：2025年9月*  
*状态：已实现*